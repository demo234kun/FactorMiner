# FactorMiner 复刻版 · 设计系统 (DESIGN.md)

> 本文件是前端视觉的唯一契约。所有颜色/字体/间距/组件 token 均由此定义，
> 禁止在代码中引入未登记的裸 hex / 任意 px。

## 1. 产品定位与视觉方向

- **定位**：策略适应与自进化平台 —— 用户生成策略 → 标准回测 → 失灵诊断 → 记忆进化 → 预警监控。
- **氛围**：**仿 ARTi 智能投研平台**（artifin.ai）。浅色高级 SaaS 质感：米白底、深棕黑文字、
  金色点缀、大标题叙事、白色圆角卡片、胶囊 CTA 按钮。简洁优雅，突出"机构级"可信度。
- **签名记忆点**：米白底 `#f6f5f3` + 深棕黑大标题 + 金色点缀 `#745313` + 白色 16px 圆角卡片 +
  深棕黑胶囊 CTA。页面留白充足，信息密度适中，视觉重心在对话生成区。
- **材料语言**：无重阴影、无玻璃拟态；靠米白/白色分层 + 细边框 + 留白建立层级。

## 2. 布局分割（沿用三栏壳，浅色化）

```
┌──────────────────────────────────────────────────────┐
│ 顶部命令栏：Logo(深棕)  菜单  搜索框  CTA「立即体验」      │
├──────────┬───────────────────────────────────────────┤
│ 左侧导航  │  主工作区：白色圆角卡片面板                  │
│ 分组菜单  │  [对话生成 卡片]  [生成结果 卡片]            │
│ 当前高亮  │  [指标卡 Sharpe/年化...]                    │
├──────────┴───────────────────────────────────────────┤
│ 底部状态栏：数据源状态 · 版本 · 时间                     │
└──────────────────────────────────────────────────────┘
```

## 3. 色彩 Token（ARti 浅色系）

| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#f6f5f3` | 页面背景（米白） |
| `--bg-panel` | `#ffffff` | 卡片/面板底（白） |
| `--bg-panel-2` | `#ebe7df` | 次级面板 / hover 底（浅米） |
| `--bg-input` | `#ffffff` | 输入框底 |
| `--border` | `#e1dcd6` | 默认边框（浅米灰） |
| `--border-strong` | `#c9c2b8` | hover/强调边框 |
| `--text` | `#171512` | 主文本（深棕黑） |
| `--text-dim` | `#4a4538` | 次级文本（深棕灰） |
| `--text-faint` | `#8a8478` | 弱标签/占位（浅棕灰） |
| `--accent` | `#201e19` | 主交互色（深棕黑，CTA/高亮） |
| `--gold` | `#745313` | 金色点缀（强调/装饰） |
| `--gold-light` | `rgba(116,83,19,.12)` | 金色浅底（标签/高亮块） |
| `--up` | `#c2410c` | 上涨/正收益（深橙红，A 股惯例暖色） |
| `--down` | `#0e7490` | 下跌/负收益（深青绿） |
| `--warn` | `#a16207` | 预警/风险提示（琥珀棕） |
| `--shadow` | `0 1px 3px rgba(23,21,18,.06)` | 面板轻投影 |

### 语义色规则
- 收益/Sharpe：`≥0` 用 `--up`（暖橙），`<0` 用 `--down`（深青绿）。
- 动作：`normal`= `--text-dim`，`risk_warn` = `--warn`，`evolve` = `--up`。
- accent（深棕黑）用于 CTA 按钮、活动导航、链接 hover。
- 金色只作点缀（logo、标签、强调数字），不用于数据表达。

## 4. 字体 Token

| Token | 值 | 用途 |
|---|---|---|
| `--font-ui` | `system-ui, -apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif` | 界面文本 |
| `--font-mono` | `"Cascadia Code", Consolas, monospace` | 公式/数字/代码 |

- 大标题：`48px/700`（hero），`32px/700`（section），`18px/600`（卡片标题）。
- 数字一律 `font-variant-numeric: tabular-nums`，保证指标列对齐。

## 5. 间距与圆角

| Token | 值 |
|---|---|
| `--space-1` | 4px |
| `--space-2` | 8px |
| `--space-3` | 12px |
| `--space-4` | 16px |
| `--space-5` | 24px |
| `--space-6` | 40px |
| `--radius-sm` | 8px |
| `--radius-md` | 12px |
| `--radius-lg` | 16px |
| `--radius-full` | 999px（胶囊按钮） |
| 页面最大宽 | 1200px，居中 |

## 6. 基础组件与状态

- **主 CTA 按钮**：`--accent`（深棕黑）底 + 白字 + 胶囊圆角；hover 上移 1px + 轻投影；active `scale(.98)`。
- **次按钮**：`--bg-panel-2`（浅米）底 + 深棕字 + 12px 圆角 + 1px 细边框。
- **输入框**：白底 + 1px 浅边框 + 12px 圆角；focus 边框转深棕 + 金色浅底外发光。
- **卡片（面板）**：白底 + 1px `--border` + `--radius-lg`(16px) + 轻投影；**无渐变标题栏**，
  改用"小标题 + 金色分隔线"。
- **左侧导航**：固定宽 200px；分组标题 `--text-faint` 小号；当前项金色浅底 + 深棕文字。
- **顶部命令栏**：高 56px，sticky，半透明白底（`rgba(246,245,243,.9)`）+ 底部细边框；
  右 CTA「立即体验」胶囊按钮。
- **表格**：表头 `--text-faint` 小号；行 hover `--bg-panel-2`；数字列 mono + tabular；无竖边框。
- **图表**：SVG 线宽 2，主曲线 `--accent`、回撤 `--down`；背景白 + 浅灰虚线网格；
  坐标轴标签 `--text-faint`；数据图表保持静态。

## 7. 页面结构（应用壳）

- 三栏壳（`_PAGE_FRAME`）：顶部命令栏 + 左侧导航 + `<main>` 主区 + 底部状态栏。
- 主区 `max-width: 1200px` 居中，卡片网格 `grid-template-columns: repeat(auto-fit,minmax(420px,1fr))`。
- 移动端（<900px）：左侧导航折叠为顶部横向菜单，卡片单列。

## 8. 无障碍与约束

- 所有可交互元素有 focus ring（深棕 2px 偏移）。
- 文本对比度：主文本 #171512 on #f6f5f3 ≥ 10:1；次级 #4a4538 ≥ 6:1。
- 颜色不只表达状态：风险提示同时带文字标记（如 "风险" 标签），不只靠颜色。
- 动画仅 `transform/opacity`，不做布局属性动画；无装饰性微动效。

## 9. 接受的设计债（本轮）

- 服务端渲染 + 内联 SVG，无前端框架 —— 图表交互（tooltip/缩放）暂不做，保持静态高可读。
- 不引入外部字体/CDN（离线可用优先），使用系统字体栈（与 ARTi 一致）。
- 无前端测试框架，视觉验证走 Playwright 截图 + 人工/visual-qa 双检。
